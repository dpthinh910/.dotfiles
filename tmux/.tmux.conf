# Use Ctrl+A as the prefix key
set -g prefix C-A
unbind C-b
bind C-a send-prefix

# Use Vim shortcuts
setw -g mode-keys vi

# Make `Ctrl-A + R` to reload config file
unbind r
bind r source-file ~/.tmux.conf